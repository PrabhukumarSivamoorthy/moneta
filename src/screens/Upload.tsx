import ScreenStub from "../components/ScreenStub";

export default function Upload() {
  return <ScreenStub title="Upload" note="Upload & review — nothing enters the ledger until you confirm." phase={1} />;
}
